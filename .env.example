# Optional semantic review: use a Groq FREE-plan account. Never commit real keys.
# Create your own key at https://console.groq.com/keys; do not upgrade billing.
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-20b
# Optional read-only GitHub token for increased public API quota.
GITHUB_TOKEN=
